# Changelogs

## 项目介绍
本文件夹下准备开发一个称作“女巫毒药”的游戏。

## 游戏规则
游戏类似于海战棋，供两位玩家进行游玩。开始游戏前，两位玩家需要各自在棋盘下按规则放置隐藏的不同形状的棋子。游戏正式开始后，两位玩家轮流进行操作，猜测对方棋子的位置。直至一方玩家猜中所有棋子为止。

### 经典模式规则
* **棋子**：仅有1*1的方块，代表“毒药”。当对方玩家选取了“毒药”所在的位置时，游戏结束，对方玩家被毒死。
* **普通方格**：显示为“面包”。
* **棋盘**：两位玩家共享同一个棋盘，轮流操作时，不能选已经被选过的方格。大小为5*5。

### 海战棋规则
* **棋子**：由1*5、1*4、1*3、1*3四块组成，代表“船”。对方玩家选取了“船“所在的位置时，标记”被击中“。当一名所有由船的格子被击中之后，该玩家落败。
* **普通方格**：显示为空格。选取了没有“船”的方格后，显示“没打中”。
* **棋盘**：两位玩家各自有一个棋盘，大小为10*10。

## 代码结构
建议采用前后端分离的架构，使用 WebSocket 进行实时通信。

### 前端
使用现代前端框架（如 Vue 或 React）构建。

* **`api/`**: 存放与后端通信的模块。
    * `websocket.js`: 封装 WebSocket 客户端逻辑，负责实时通信。
* **`components/`**: 可复用的UI组件。
    * `Board.jsx`: 棋盘组件，可根据props适配不同尺寸和模式。
    * `Cell.jsx`: 棋盘格子组件，处理点击事件并显示不同状态 (空、面包、毒药、击中、未击中)。
    * `GameInfo.jsx`: 游戏信息面板，显示回合、提示和游戏结果。
* **`views/`**: 应用的主要页面。
    * `Home.jsx`: 游戏主页，提供模式选择入口。
    * `GameRoom.jsx`: 游戏房间主视图，整合棋盘和信息组件，管理对局状态。
* **`hooks/` / `store/`**: 全局状态管理。
    * `useGameStore.js`: 集中管理游戏ID、玩家状态、棋盘数据等。

### 后端
使用 Node.js (搭配 Express 和 Socket.IO) 或 Python (搭配 FastAPI)。

* **`server.js` / `main.py`**: 服务器主入口，初始化应用和 WebSocket 服务。
* **`game/`**: 核心游戏逻辑目录。
    * `GameManager.js`: 管理所有活跃的游戏房间实例。
    * `Game.js`: 单个游戏房间的类，处理玩家加入、棋子放置、攻击判定和回合管理。
* **`network/`**: 网络通信层。
    * `socketHandler.js`: 专门处理 WebSocket 的所有事件，如 `createGame`, `joinGame`, `makeMove`, `placePieces` 等，并调用`Game`实例进行处理。

## 开发计划

### v0.1: “经典模式”核心玩法实现 (MVP)
此版本目标是完成“经典模式”的最小可行产品。
* **后端**:
    * 搭建基础 WebSocket 服务器，实现游戏房间的创建和双人加入。
    * 实现 5x5 棋盘的“毒药”放置逻辑和规则验证。
    * 实现轮流选择方格的核心玩法及胜负判断。
* **前端**:
    * 创建 5x5 棋盘视图及交互功能。
    * 实现放置“毒药”和轮流点击的操作。
    * 能根据后端数据，正确显示“面包”状态和游戏结束画面。

### v0.2: “海战棋模式”玩法实现
在 v0.1 基础上，扩展出“海战棋模式”。
* **后端**:
    * 扩展游戏逻辑以支持 10x10 的双棋盘模式。
    * 实现四种船只的放置规则验证（防重叠、防越界）。
    * 实现“未击中”、“击中”、“击沉”的攻击判定逻辑。
* **前端**:
    * 开发用于放置多种船只的 UI 界面。
    * 实现双棋盘视图：一个显示我方船只，一个记录对敌方攻击。
    * 根据后端返回结果，实时更新棋盘格子的状态。

### v0.3: 完善游戏体验和UI
优化整体流程和视觉效果。
* **后端**:
    * 增加玩家断线重连支持。
    * 代码重构，优化性能和结构。
* **前端**:
    * 设计游戏大厅/主菜单，允许玩家选择游戏模式。
    * 美化UI，增加击中、游戏胜利/失败等动画效果。
    * 增加更清晰的游戏流程引导和提示。

### v1.0: 正式版与部署
准备发布。
* 进行全面的功能测试和 Bug 修复。
* 准备前后端部署脚本（如使用 Docker）。
* 将应用部署到线上服务器。

## 开发日志
在这里补充每个版本的开发日志，版本号使用vX.Y.Z的形式，其中X为大版本号，Y为小版本号，Z为修订号。